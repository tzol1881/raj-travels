import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class TaxTypeService {

  constructor() { }
  port = 'https://www.icoy.co.in/transport/api'
  getTaxTypes(){
    return  fetch(`${this.port}/getTaxTypeList`,{
      method: "POST",
      mode: "cors",
      cache: "no-cache",
      credentials: "same-origin",
      headers: {
        "Content-Type": "application/json"
      },
      redirect: "follow",
      referrerPolicy: "no-referrer",
      // body: JSON.stringify(data),
    })
  }

  getTaxTypeCombo(){
    return  fetch(`${this.port}/getTaxTypeCombo`,{
      method: "POST",
      mode: "cors",
      cache: "no-cache",
      credentials: "same-origin",
      headers: {
        "Content-Type": "application/json"
      },
      redirect: "follow",
      referrerPolicy: "no-referrer",
      // body: JSON.stringify(data),
    })
  }

  addTaxType(data: any){
    return  fetch(`${this.port}/addTaxType`,{
      method: "POST",
      mode: "cors",
      cache: "no-cache",
      credentials: "same-origin",
      headers: {
        "Content-Type": "application/json"
      },
      redirect: "follow",
      referrerPolicy: "no-referrer",
      body: JSON.stringify(data),
    })
  }

  editTaxType(data: any){
    return  fetch(`${this.port}/editTaxType`,{
      method: "POST",
      mode: "cors",
      cache: "no-cache",
      credentials: "same-origin",
      headers: {
        "Content-Type": "application/json"
      },
      redirect: "follow",
      referrerPolicy: "no-referrer",
      body: JSON.stringify(data),
    })
  }

  deleteTaxTypeMaster(data: any){
    return  fetch(`${this.port}/deleteTaxTypeMaster`,{
      method: "POST",
      mode: "cors",
      cache: "no-cache",
      credentials: "same-origin",
      headers: {
        "Content-Type": "application/json"
      },
      redirect: "follow",
      referrerPolicy: "no-referrer",
      body: JSON.stringify(data),
    })
  }
}
